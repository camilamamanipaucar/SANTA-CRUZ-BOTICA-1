server:
  port: 7070

spring:
  application:
    name: santacruz-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/camilamamanipaucar/SANTA-CRUZ-BOTICA-1.git
          searchPaths: santacruz-config-data
          default-label: Principal
  security:
    user:
      name: camila
      password: rosa
